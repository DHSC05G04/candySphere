<!-- Função para pegar core aleatorias -->
<script> function background(teste){
    const cores =[];
    teste.forEach(element => {
        cores.push(`rgba(${Math.floor(Math.random() * 255)}, ${Math.floor(Math.random() * 255)}, ${Math.floor(Math.random() * 255)}, 0.7)`);
    });
    return cores;
   } </script>

 <!-- Começo do grafico Despesa x Receita -->
<script>
    var cor = background(dbData.data);
    var ctx = document.getElementById('pizza-despesa').getContext('2d');
    var myChart = new Chart(ctx, {
    type: 'doughnut',
    data: {
        labels: dbData.label,
        datasets: [{
            
            data: dbData.data,
            backgroundColor: cor,
            borderColor: cor,
            borderWidth: 1
        }]
    },
    options: {
        
    }
});
</script> 
